%% \makeatletter
%% \renewcommand{\@seccntformat}[1]{}
%% \makeatother
%% \PassOptionsToPackage{hyphens}{url}
%% \PassOptionsToPackage{usenames,dvipsnames,svgnames*,x11names*}{xcolor}


%% documentclass
\renewcommand{\linethickness}{0.05pt}
\documentclass[UTF8,17pt,english,a4,handout]{book}

\usepackage{textcomp} % provide euro and other symbols
\usepackage{parskip} % sensible indent and break in paragraphs
\usepackage{pgfpages}

%% unordered list character
\usepackage{pifont}
\renewcommand\labelitemi{\ding{47}}

\usepackage{geometry}
 \geometry{
 a4paper,
 total={170mm,257mm},
 left=30mm,
 right=30mm,
 top=24mm,
 bottom=24mm,
 }


\usepackage{color}
\usepackage{fancyvrb}
\usepackage{xcolor}
\usepackage{listings}

\definecolor{titlecolor}{RGB}{51,48,0}


\usepackage{fancyhdr}
\pagestyle{fancy}
\usepackage{beamerarticle}
\usepackage{wrapfig}
\usepackage{setspace}
\setstretch{1.25}

\usepackage{fontspec}

\usepackage{titling}
%% \usepackage[condensed]{roboto}
\usepackage[default]{roboto}

\usepackage[T1]{fontenc}
%% \renewcommand{\rmdefault}{condensed}
%% \renewcommand\thesection{}
%% \renewcommand\thesubsection{}
\usepackage{FiraMono}
\setmonofont{Fira Mono}

\definecolor{MyBlue}{rgb}{0.1,0.1,1}

\usepackage{sectsty}
\chapterfont{\color{redz}}
\sectionfont{\color{MyBlue}}

\usepackage{caption}

% These lines are needed to make table captions work with longtable:
\makeatletter
\def\fnum@table{\tablename~\thetable}
\makeatother

\usepackage{graphicx,grffile}

%% \setkeys{Gin}{width=\ScaleWidthIfNeeded,height=\ScaleHeightIfNeeded,keepaspectratio}%


% set default figure placement to htbp
\makeatletter
\def\fps@figure{htbp}
\makeatother

\setlength{\emergencystretch}{3em}  % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\usepackage[colorlinks]{hyperref}
\usepackage{longtable,booktabs}
\usepackage{footnote}
\makesavenoteenv{longtable}

\setcounter{secnumdepth}{-2}

\hypersetup{
  %% pdfkeywords={$for(keywords)$$keywords$$sep$, $endfor$},
  pdftoolbar=true, % show Acrobat’s toolbar?
  pdfmenubar=true, % show Acrobat’s menu?
  pdffitwindow=true, % window fit to page when opened
  colorlinks=false,
  breaklinks=false,
  pdfcreator={LaTeX via pandoc},
}

%% \urlstyle{same}

\usepackage{framed}
\VerbatimFootnotes

$if(highlighting-macros)$
$highlighting-macros$
$endif$

\usepackage{graphicx}

$for(header-includes)$
$header-includes$
$endfor$


\usepackage{etoolbox}
\makeatletter
\providecommand{\subtitle}[1]{% add subtitle to \maketitle
  \apptocmd{\@title}{\par {\large #1 \par}}{}{}
}
\makeatother

\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother

\usepackage[raggedright]{titlesec}

\newcommand{\passthrough}[1]{\lstset{mathescape=false}#1\lstset{mathescape=true}}

\usepackage{titlesec}

\titleformat*{\chapter}{\robotocondensed\color{titlecolor}\fontsize{20}{22}\Huge\selectfont\bfseries}
\titleformat*{\section}{\robotocondensed\color{titlecolor}\fontsize{16}{16}\Large\selectfont\bfseries}
\titleformat*{\subsection}{\robotocondensed\color{titlecolor}\fontsize{14}{16}\Large\selectfont\bfseries}
%% \titleformat*{\subsection}{\large\bfseries\sffamily\color{redz}}

\begin{document}
$for(include-before)$
$include-before$
$endfor$

$body$

$for(include-after)$
$include-after$
$endfor$
\end{document}
